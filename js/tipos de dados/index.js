//String, number, undefined, null, boolean, symbol
const nome =  Antony // String
const nomel ="Antony"// String
const nome2 =`Luiz` // string
const numl = l0; // number
const num2 = 10.52; // number
let nomeALuno; // undefined -> nao aponta pra local nenhum  na memoria
const sobrenomeAluno = null; // Nul0 -> não aponta pra local nenhum na memória
const aprovado = false // Boolean (verdadeiro ou falso)